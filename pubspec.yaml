name: intl_phone_field
description: A customised Flutter TextFormField to input international phone number along with country code.
version: 1.0.0
homepage: https://github.com/Gnac05/intl_phone_field

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^3.0.1
  flutter_test:
    sdk: flutter
  test: ^1.17.12

flutter:
  assets:
    - assets/flags/
